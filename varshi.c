#include<stdio.h>
int main()
{
	int p,q,r,s;
	printf("\ninput the even integer:");
	scanf("%d",&q);
	printf("\ninput the integer:");
	scanf("%d",&q);
	printf("\ninput the positive integer:");
	scanf("%d",&r);
	printf("\ninput the positive integer:");
	scanf("%d",&s);
	if ((q>r)&&(s>p)&&((r+s)>(p+q))&&(r>0)&&(s>0)&&(p%2==0))
	{
		printf("\ncorrect values\n");
	}
	else
	{
		printf("\nwrong values\n");
}
		return;
	}
	  

